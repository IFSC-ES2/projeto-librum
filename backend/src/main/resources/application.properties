spring.application.name=backend

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/librum}
spring.datasource.username=${DB_USER:librum}
spring.datasource.password=${DB_PASSWORD:librum}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
jwt.secret=${JWT_SECRET:dev-secret-key-troque-em-producao}
jwt.expiration=86400000
