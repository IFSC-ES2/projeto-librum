services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: librum
      POSTGRES_USER: librum
      POSTGRES_PASSWORD: librum
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: